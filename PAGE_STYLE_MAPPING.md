# 页面-样式映射表

本文档整理了应用中所有页面使用的UI样式，方便快速查找和统一调整。

## 📋 样式编号索引

### 输入框样式
- **S1** - 标准文本输入框样式 (`StandardTextFieldStyle`) ⭐ 默认
- **S2** - 金额输入框样式 (`AmountInputFieldStyle`) 🔸 特殊场景
- **S3** - 日期选择器样式 (`DatePickerFieldStyle`) 🔸 特殊场景

### 下拉框样式
- **S4** - 标准下拉框样式 (`StandardDropdownStyle`) ⭐ 默认

### 选择组件样式
- **S5** - 复选框样式 (`CheckboxStyle`) ⭐ 默认
- **S6** - 开关样式 (`SwitchStyle`) ⭐ 默认
- **S7** - 单选按钮样式 (`RadioButtonStyle`) ⭐ 默认
- **S8** - 滑块样式 (`SliderStyle`) ⭐ 默认
- **S9** - 分段选择器样式 (`SegmentedControlStyle`) 🔸 特殊场景
- **S10** - 标签芯片样式 (`ChipStyle`) 🔸 特殊场景

### 按钮样式
- **S11** - 主要按钮样式 (`PrimaryButtonStyle`) ⭐ 默认
- **S12** - 文本按钮样式 (`TextButtonStyle`) ⭐ 默认
- **S13** - 轮廓按钮样式 (`OutlinedButtonStyle`) 🔸 特殊场景
- **S14** - 图标按钮样式 (`IconButtonStyle`) ⭐ 默认
- **S15** - 浮动操作按钮样式 (`FloatingActionButtonStyle`) 🔸 特殊场景
- **S16** - 带图标按钮样式 (`IconLabelButtonStyle`) 🔸 特殊场景

### 对话框样式
- **S17** - 确认对话框样式 (`ConfirmationDialogStyle`) ⭐ 默认
- **S18** - 底部表单样式 (`BottomSheetStyle`) ⭐ 默认

### 标签页样式
- **S19** - 标准标签页样式 (`StandardTabStyle`) ⭐ 默认

### 提示样式
- **S20** - SnackBar提示样式 (`SnackBarStyle`) ⭐ 默认
- **S21** - 玻璃通知样式 (`GlassNotificationStyle`) 🔸 特殊场景

---

## 📊 页面-样式映射表

### 主导航模块

#### MainNavigationScreen
- **S11** - 主要按钮样式（底部导航）

---

### 家庭信息模块 (Family Info)

#### FamilyInfoHomeScreen
- **S11** - 主要按钮样式（操作按钮）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）

#### SalaryIncomeSetupScreen
- **S2** - 金额输入框样式（`AmountInputField` - 基本工资、奖金等）
- **S8** - 滑块样式（`Slider` - 薪资比例）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）
- **S20** - SnackBar提示样式（保存成功/失败提示）

#### SalaryPreviewScreen
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）
- **S13** - 轮廓按钮样式（预览按钮）
- **S20** - SnackBar提示样式（保存提示）

#### WalletManagementScreen
- **S11** - 主要按钮样式（添加钱包）
- **S14** - 图标按钮样式（删除、编辑）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）

#### AccountDetailScreen
- **S14** - 图标按钮样式（删除、编辑、设置）
- **S19** - 标准标签页样式（`TabBar` / `TabBarView`）
- **S18** - 底部表单样式（`showModalBottomSheet`）
- **S20** - SnackBar提示样式（操作提示）

#### AccountEditScreen
- **S1** - 标准文本输入框样式（账户名称、余额等）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）

#### AddWalletScreen
- **S1** - 标准文本输入框样式（钱包名称）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）

#### AssetManagementScreen
- **S11** - 主要按钮样式（添加资产）
- **S14** - 图标按钮样式（删除、编辑）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认）
- **S20** - SnackBar提示样式（操作提示）

#### AssetDetailScreen
- **S1** - 标准文本输入框样式（资产信息编辑）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）

#### AssetEditScreen
- **S1** - 标准文本输入框样式（资产信息）
- **S11** - 主要按钮样式（保存按钮）
- **S13** - 轮廓按钮样式（取消按钮）

#### AddAssetFlowScreen
- **S11** - 主要按钮样式（添加资产）
- **S13** - 轮廓按钮样式（取消按钮）
- **S14** - 图标按钮样式（删除、编辑）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S18** - 底部表单样式（`showModalBottomSheet`）
- **S20** - SnackBar提示样式（操作提示）

#### AddAssetSheet
- **S1** - 标准文本输入框样式（资产名称、金额）
- **S10** - 标签芯片样式（`FilterChip` - 子分类选择）
- **S11** - 主要按钮样式（保存按钮）
- **S13** - 轮廓按钮样式（取消按钮）
- **S18** - 底部表单样式（`showModalBottomSheet`）

#### EditAssetSheet
- **S1** - 标准文本输入框样式（资产信息）
- **S11** - 主要按钮样式（保存按钮）
- **S13** - 轮廓按钮样式（取消按钮）

#### PropertyDetailScreen
- **S1** - 标准文本输入框样式（房产信息）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）
- **S16** - 带图标按钮样式（`TextButton.icon`）
- **S20** - SnackBar提示样式（操作提示）

#### FixedAssetDetailScreen
- **S1** - 标准文本输入框样式（固定资产信息）
- **S6** - 开关样式（`SwitchListTile` - 折旧开关）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）

#### AssetValuationSetupScreen
- **S1** - 标准文本输入框样式（估值信息）
- **S6** - 开关样式（`SwitchListTile`）
- **S7** - 单选按钮样式（`Radio` - 折旧方法选择）
- **S11** - 主要按钮样式（保存按钮）
- **S13** - 轮廓按钮样式（取消按钮）

#### ClearanceHomeScreen
- **S1** - 标准文本输入框样式（期末余额输入）
- **S11** - 主要按钮样式（创建会话、提交）
- **S14** - 图标按钮样式（删除）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认）
- **S18** - 底部表单样式（`showModalBottomSheet`）
- **S20** - SnackBar提示样式（操作提示）

#### PeriodDifferenceAnalysisScreen
- **S1** - 标准文本输入框样式（金额输入）
- **S11** - 主要按钮样式（添加交易、完成清账）
- **S12** - 文本按钮样式（取消）
- **S13** - 轮廓按钮样式（`OutlinedButton.icon`）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认、完成清账确认）
- **S20** - SnackBar提示样式（操作提示）

#### PeriodSummaryScreen
- **S11** - 主要按钮样式（生成总结）
- **S14** - 图标按钮样式（返回）
- **S18** - 底部表单样式（`showModalBottomSheet`）
- **S20** - SnackBar提示样式（操作提示）

---

### 财务规划模块 (Financial Planning)

#### FinancialPlanningHomeScreen
- **S11** - 主要按钮样式（创建计划）
- **S13** - 轮廓按钮样式（`OutlinedButton.icon` - 计划操作）
- **S18** - 底部表单样式（`showModalBottomSheet` - 创建计划选择）
- **S20** - SnackBar提示样式（操作提示）

#### BudgetManagementScreen
- **S11** - 主要按钮样式（创建预算、操作按钮）
- **S14** - 图标按钮样式（删除、编辑）
- **S15** - 浮动操作按钮样式（`FloatingActionButton` - 添加预算）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认）
- **S19** - 标准标签页样式（`TabBar` / `TabBarView`）

#### CreateBudgetScreen
- **S1** - 标准文本输入框样式（预算名称、金额）
- **S6** - 开关样式（`SwitchListTile` - 自动分配）
- **S8** - 滑块样式（`Slider` - 分配比例）
- **S11** - 主要按钮样式（创建按钮）
- **S12** - 文本按钮样式（取消按钮）

#### EnvelopeBudgetDetailScreen
- **S11** - 主要按钮样式（操作按钮）
- **S13** - 轮廓按钮样式（`OutlinedButton.icon`）
- **S14** - 图标按钮样式（返回、设置）
- **S16** - 带图标按钮样式（`ElevatedButton.icon`）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认）
- **S18** - 底部表单样式（`AppAnimations.showAppModalBottomSheet`）
- **S19** - 标准标签页样式（`TabBar` / `TabBarView`）
- **S20** - SnackBar提示样式（操作提示）

#### CreateIncomePlanScreen
- **S1** - 标准文本输入框样式（计划名称、金额）
- **S6** - 开关样式（`Switch` - 自动执行）
- **S11** - 主要按钮样式（创建按钮）
- **S13** - 轮廓按钮样式（取消按钮）
- **S17** - 确认对话框样式（`AlertDialog` - 从工资创建确认）
- **S20** - SnackBar提示样式（操作提示）

#### CreateExpensePlanScreen
- **S1** - 标准文本输入框样式（计划名称、金额）
- **S6** - 开关样式（`Switch` - 自动执行、关联预算）
- **S11** - 主要按钮样式（创建按钮）
- **S13** - 轮廓按钮样式（取消按钮、操作按钮）
- **S20** - SnackBar提示样式（操作提示）

#### MortgageCalculatorScreen
- **S1** - 标准文本输入框样式（贷款信息）
- **S2** - 金额输入框样式（`AmountInputField` - 贷款金额、月供）
- **S8** - 滑块样式（`Slider` - 利率）
- **S11** - 主要按钮样式（计算按钮）
- **S13** - 轮廓按钮样式（重置按钮）
- **S17** - 确认对话框样式（`AlertDialog` - 保存确认）
- **S20** - SnackBar提示样式（操作提示）

#### RepaymentHistoryScreen
- **S13** - 轮廓按钮样式（`OutlinedButton.icon`）
- **S20** - SnackBar提示样式（操作提示）

#### SmartBudgetGuidanceScreen
- **S5** - 复选框样式（`Checkbox` - 选择项）
- **S11** - 主要按钮样式（生成建议）
- **S12** - 文本按钮样式（取消）
- **S13** - 轮廓按钮样式（取消）

---

### 交易流水模块 (Transaction Flow)

#### TransactionFlowHomeScreen
- **S15** - 浮动操作按钮样式（`EnhancedFloatingActionButton` - 添加交易）

#### AddTransactionScreen
- **S1** - 标准文本输入框样式（`TextFormField` - 描述、备注）
- **S6** - 开关样式（`SwitchListTile` - 定期交易、草稿）
- **S11** - 主要按钮样式（保存按钮）
- **S12** - 文本按钮样式（取消按钮）

#### TransactionRecordsScreen
- **S11** - 主要按钮样式（筛选、操作）
- **S20** - SnackBar提示样式（操作提示）

#### TransactionListScreen
- **S10** - 标签芯片样式（`FilterChip` - 筛选标签）

#### TransactionManagementScreen
- **S19** - 标准标签页样式（`TabBar` / `TabBarView`）

#### TransactionDetailScreen
- **S11** - 主要按钮样式（编辑、删除）
- **S12** - 文本按钮样式（取消）

---

### 设置和配置模块

#### SettingsScreen
- **S11** - 主要按钮样式（操作按钮）
- **S20** - SnackBar提示样式（操作提示）

#### DeveloperModeScreen
- **S6** - 开关样式（`Switch` - 文件日志开关）
- **S11** - 主要按钮样式（操作按钮）
- **S17** - 确认对话框样式（`AlertDialog` - 确认操作）

#### AiConfigScreen
- **S1** - 标准文本输入框样式（`TextField` - API密钥、Base URL）
- **S6** - 开关样式（`SwitchListTile` - AI启用开关）
- **S7** - 单选按钮样式（`RadioListTile<AiProvider>` - 提供商选择）
- **S11** - 主要按钮样式（保存、验证按钮）
- **S12** - 文本按钮样式（取消按钮）
- **S17** - 确认对话框样式（`AlertDialog` - 删除确认）
- **S21** - 玻璃通知样式（`GlassNotification` - 错误提示）

#### PersonalScreen
- **S20** - SnackBar提示样式（操作提示）

---

### 其他页面

#### OnboardingScreen
- **S11** - 主要按钮样式（下一步、完成）

#### DashboardScreen
- **S11** - 主要按钮样式（操作按钮）

#### EnvelopeBudgetDetailScreen (screens包)
- **S19** - 标准标签页样式（`TabBar` / `TabBarView`）

#### TaxCalculationModeSelectionScreen
- **S7** - 单选按钮样式（计算模式选择）
- **S11** - 主要按钮样式（确认按钮）

#### MonthlyWalletScreen
- **S11** - 主要按钮样式（操作按钮）

#### AssetListScreen
- **S19** - 标准标签页样式（`AssetCategoryTabBar`）

---

## 📈 样式使用统计

### 最常用的样式（Top 5）
1. **S11** - 主要按钮样式（90%+ 页面使用）
2. **S20** - SnackBar提示样式（80%+ 页面使用）
3. **S1** - 标准文本输入框样式（60%+ 表单页面使用）
4. **S12** - 文本按钮样式（50%+ 表单页面使用）
5. **S17** - 确认对话框样式（40%+ 页面使用）

### 特殊样式使用情况
- **S2** - 金额输入框样式：仅在金额输入场景（薪资设置、贷款计算等）
- **S10** - 标签芯片样式：仅在筛选/选择场景（资产分类、交易筛选）
- **S15** - 浮动操作按钮样式：仅在添加操作场景（添加交易）
- **S21** - 玻璃通知样式：仅在AI配置页面使用

---

## 🎯 样式统一建议

### 高优先级统一
1. **按钮样式统一**：所有主要操作按钮使用 S11，次要操作使用 S12
2. **提示样式统一**：所有操作反馈使用 S20（SnackBar）
3. **对话框统一**：所有确认操作使用 S17（统一通知系统）

### 中优先级统一
1. **输入框统一**：表单输入统一使用 S1（标准文本输入框）
2. **底部表单统一**：所有底部表单使用 S18（AppAnimations.showAppModalBottomSheet）

### 低优先级（保持现状）
1. **特殊样式**：金额输入框（S2）、标签芯片（S10）等特殊场景保持现状

---

## 📝 快速查找

### 按样式查找页面
- **S1（标准文本输入框）**：所有表单页面
- **S2（金额输入框）**：薪资设置、贷款计算、资产添加
- **S11（主要按钮）**：几乎所有页面
- **S19（标签页）**：账户详情、预算管理、交易管理、信封预算详情

### 按页面查找样式
- **表单页面**：S1 + S11 + S12 + S20
- **列表页面**：S11 + S14 + S20
- **详情页面**：S1 + S11 + S12 + S20
- **设置页面**：S1 + S6 + S7 + S11 + S12 + S20

---

**最后更新：** 2024-12-19
**维护者：** AI Assistant

