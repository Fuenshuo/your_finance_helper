/// 🌊 Flux Ledger - 默认主入口点
///
/// 指向 Flux Ledger 应用的统一入口
/// 使用 flutter run 默认启动 Flux Ledger
library;

import 'package:your_finance_flutter/main_flux.dart' as flux;

/// 默认主入口点 - 启动 Flux Ledger 应用
void main() {
  flux.main();
}
